const { obtainSubSequences } = require('./sort-sequences');

describe('obtainSubSequences', () => {
  it('should return obtainSubSequences', () => {
    expect(obtainSubSequences()).toBe('hello');
  });
});
